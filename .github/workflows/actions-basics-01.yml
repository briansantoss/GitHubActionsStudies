name: Use action 'greet'

on:
    workflow_dispatch:

jobs:
    main:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: ./.github/actions/greet
              with:
                name: Sasuke Uchiha