name: Greet action
description: Greets someone

inputs:
  name:
    required: true
    default: John Doe
    type: string
    description: Person to greet

runs:
  using: composite
  steps:
    - run: echo ${{inputs.name}}
      shell: bash